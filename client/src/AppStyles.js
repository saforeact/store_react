import { makeStyles } from "@material-ui/core";

export default makeStyles({
  app_wrapper: {
    display: "flex",
    flexDirection: "column",
    gap: 10,
  },
  app_header: {},
  app_contentBox: {},
  app_sideBar: {
    width: 200,
  },
});
